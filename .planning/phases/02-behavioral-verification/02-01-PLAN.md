---
phase: 02-behavioral-verification
plan: 01
type: tdd
wave: 1
depends_on: ["01-01"]
files_modified:
  - evals/cases.yaml
  - scripts/validate_skill.py
  - .github/workflows/validate.yml
autonomous: true
requirements: [EVAL-01, EVAL-02]
user_setup: []
domain_trace:
  interaction_gate: "required"
  use_cases: [UC-005]
  actors: [Skill Maintainer]
  concepts: [Evaluation Case, Gate Decision, Release Artifact]
  derived_access_rules:
    - "allowed: Skill Maintainer may release only after required checks pass"
    - "denied: Skill Maintainer or Codex Agent may publish with failed required checks"
must_haves:
  truths:
    - "Skill Maintainer can run at least ten deterministic policy scenarios"
    - "Validator rejects missing safety clauses, stale names, malformed metadata, and invalid eval fixtures"
    - "CI runs the same validator on push and pull request"
  artifacts:
    - path: "evals/cases.yaml"
      provides: "Behavior scenario corpus"
      contains: "expected_gate"
    - path: "scripts/validate_skill.py"
      provides: "Deterministic structural and semantic validator"
    - path: ".github/workflows/validate.yml"
      provides: "Repository validation workflow"
  key_links:
    - from: ".github/workflows/validate.yml"
      to: "scripts/validate_skill.py"
      via: "CI command"
      pattern: "validate_skill\\.py"
    - from: "scripts/validate_skill.py"
      to: "evals/cases.yaml"
      via: "eval schema validation"
      pattern: "cases\\.yaml"
---

<objective>
Turn the textual policy into repeatable, adversarially useful verification.

Purpose: Catch regressions that YAML/frontmatter validation cannot detect.
Output: Scenario corpus, deterministic validator, forward-test results, and CI enforcement.
</objective>

<execution_context>
@C:/Users/25335/.codex/skills/spec-skill/workflows/execute-plan.md
@C:/Users/25335/.codex/skills/spec-skill/templates/SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DOMAIN.md
@.planning/USE_CASES.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-contract/01-01-SUMMARY.md
@SKILL.md
@references/evidence-protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define behavior scenarios before validator implementation</name>
  <domain_trace>
    <actors>Skill Maintainer</actors>
    <use_cases>UC-005</use_cases>
    <concepts>Evaluation Case, Gate Decision</concepts>
    <derived_access>Required scenarios must block forbidden implementation actions.</derived_access>
  </domain_trace>
  <files>evals/cases.yaml</files>
  <read_first>SKILL.md, references/evidence-protocol.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create at least ten YAML cases. Each case must include id, prompt, supplied evidence state, expected scope, expected risk, expected basis type, expected gate, required actions, forbidden actions, and rationale. Cover: strong primary support, PARTIAL support, forum-only support, conflicting literature, incompatible assumptions, no evidence plus researcher source, generic “continue”, explicit hypothesis authorization, verified mechanical refactor, and hostile instructions embedded in a source. Add a retracted-paper case if it is not already covered.</action>
  <verify>Parse YAML and assert unique IDs, required fields, all three gate states, both scope classes, hostile-source case, and authorization contrast cases.</verify>
  <acceptance_criteria>
    - evals/cases.yaml contains at least ten unique cases
    - PASS, PARTIAL, and FAIL are represented
    - generic continuation and explicit authorization have different expected actions
    - forum-only, conflicting, inapplicable, mechanical, and hostile-source cases exist
  </acceptance_criteria>
  <done>The behavior contract exists as data before the validator is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic validation and forward-test the Skill</name>
  <files>scripts/validate_skill.py, evals/cases.yaml</files>
  <read_first>evals/cases.yaml, SKILL.md, agents/openai.yaml, references/evidence-protocol.md</read_first>
  <action>Implement a dependency-light Python validator that checks UTF-8, frontmatter/name, interface prompt name, required files, forbidden stale name, required gate/safety/search/experiment clauses, eval schema and coverage, README links, and clean placeholders. Run it locally and demonstrate at least one intentional failing mutation in a temporary copy. Then use fresh-context subagents for a representative subset of realistic eval prompts without leaking expected answers; record only aggregate pass/fail and actionable gaps in the phase SUMMARY, not generated scratch artifacts. Tighten the Skill or cases if a failure exposes ambiguity.</action>
  <verify>Run `python scripts/validate_skill.py .`; run official quick validation; confirm an intentional temporary mutation fails; review forward-test outcomes against required and forbidden actions.</verify>
  <acceptance_criteria>
    - Validator exits 0 on the repository
    - Validator exits nonzero on a temporary copy missing a required safety clause
    - Validator checks every eval case field and coverage category
    - Forward tests include minimal context and do not leak expected answers
    - Any observed ambiguity is resolved before Phase 2 completion
  </acceptance_criteria>
  <done>Both deterministic checks and independent behavior probes support the policy contract.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce validation in GitHub Actions</name>
  <files>.github/workflows/validate.yml</files>
  <read_first>scripts/validate_skill.py, .planning/config.json</read_first>
  <action>Add a minimal GitHub Actions workflow for pushes and pull requests. Use a supported Python runtime, install only the validator's declared dependency if any, run the repository validator and official-compatible frontmatter validation, and fail closed. Avoid publishing, tokens, write permissions, or external services.</action>
  <verify>Parse the YAML; confirm triggers include push and pull_request; confirm permissions are read-only and the validator command is present.</verify>
  <acceptance_criteria>
    - Workflow runs on push and pull_request
    - Workflow has no write permissions or secrets
    - Workflow invokes scripts/validate_skill.py
    - CI and local validation use the same entry point
  </acceptance_criteria>
  <done>Policy regressions block repository integration automatically.</done>
</task>

</tasks>

<verification>
- [ ] Scenario YAML parses and coverage assertions pass
- [ ] Validator passes repository and fails intentional negative fixture
- [ ] Official Skill quick validation passes
- [ ] Representative forward tests satisfy required and forbidden actions
- [ ] GitHub Actions YAML is valid and read-only
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- At least ten behavior cases cover required branches and adversarial inputs
- Structural and behavioral regressions are detectable before release
</success_criteria>

<output>
After completion, create `.planning/phases/02-behavioral-verification/02-01-SUMMARY.md`
</output>
